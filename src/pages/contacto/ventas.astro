---
export const prerender = true;

import Layout from '../../layouts/Layout.astro';
import { getLangFromUrl } from '../../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const isEn = lang === 'en';

const title = isEn
    ? "Contact Sales | Cord by Flouvia"
    : "Contacto de Ventas | Cord by Flouvia";

const description = isEn
    ? "Get in touch with the Cord sales team to learn how we can help digitize your B2B quotes and orders."
    : "Comunícate con el equipo de ventas de Cord para descubrir cómo digitalizar tus cotizaciones y pedidos B2B.";
---

<Layout title={title} description={description}>
    <!-- Minimal Header -->
    <header class="minimal-header">
        <a href="/" class="logo-link">
            <img src="/imgs/logo-cord-navy.png" alt="Cord" class="logo-img" />
        </a>
        <a href="/app" class="dashboard-link">
            {isEn ? "Dashboard" : "Dashboard"} <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"></polyline></svg>
        </a>
    </header>

    <main class="minimal-contact-page">
        <!-- Grid Background -->
        <div class="bg-grid"></div>

        <section class="card-section">
            <div class="stepper-card reveal-mockup">
                <!-- Stepper Header -->
                <div class="stepper-header">
                    <div class="step active">
                        <span class="step-indicator"></span>
                        <span class="step-label">{isEn ? "Your email" : "Tu correo electrónico"}</span>
                    </div>
                    <div class="step">
                        <span class="step-indicator"></span>
                        <span class="step-label">{isEn ? "Your info" : "Tu información"}</span>
                    </div>
                    <div class="step">
                        <span class="step-indicator"></span>
                        <span class="step-label">{isEn ? "Let's talk" : "Hablemos"}</span>
                    </div>
                </div>
                <div class="stepper-progress">
                    <div class="progress-bar" style="width: 33.33%;"></div>
                </div>

                <!-- Step Content -->
                <div class="step-content">
                    <h1 class="step-title">{isEn ? "We'll get you to the right place" : "Te llevamos al lugar correcto"}</h1>
                    <p class="step-subtitle">{isEn ? "We just need a few details." : "Solo necesitamos algunos datos."}</p>

                    <form class="minimal-form" onsubmit="event.preventDefault(); alert('Formulario demo enviado');">
                        <div class="form-row-horizontal">
                            <label for="email">{isEn ? "Work email" : "Correo electrónico del trabajo"}</label>
                            <input type="email" id="email" name="email" placeholder="elena@ejemplo.com" required />
                        </div>
                        <div class="form-row-horizontal">
                            <label for="country">{isEn ? "Country/Region" : "País/región"}</label>
                            <div class="select-wrapper">
                                <select id="country" name="country">
                                    <option value="MX" selected>México</option>
                                    <option value="US">Estados Unidos</option>
                                    <option value="CO">Colombia</option>
                                    <option value="AR">Argentina</option>
                                    <option value="CL">Chile</option>
                                    <option value="ES">España</option>
                                    <option value="OTHER">{isEn ? "Other" : "Otro"}</option>
                                </select>
                                <svg class="select-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
                            </div>
                        </div>

                        <div class="form-actions">
                            <button type="submit" class="continue-btn">
                                {isEn ? "Continue" : "Continuar"} <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"></polyline></svg>
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </section>
        
        <!-- Gradient splash in bottom right (like screenshot) but with Cord tones -->
        <div class="bottom-gradient-splash"></div>
    </main>
</Layout>

<style is:global>
    .js-anim .reveal-mockup { opacity: 0; }
</style>

<style>
    /* Reset layout background to a soft white/gray */
    :global(body) {
        background-color: #fafbfc; 
    }

    .minimal-header {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 24px 40px;
        z-index: 100;
        background: transparent;
    }

    .logo-img {
        height: 22px;
        width: auto;
    }

    .dashboard-link {
        display: flex;
        align-items: center;
        gap: 6px;
        font-size: 0.95rem;
        font-weight: 600;
        color: var(--color-blue-deep, #0a192f);
        text-decoration: none;
        transition: opacity 0.2s;
    }

    .dashboard-link:hover {
        opacity: 0.7;
    }

    .dashboard-link svg {
        width: 16px;
        height: 16px;
    }

    .minimal-contact-page {
        position: relative;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        overflow: hidden;
    }

    /* Grid Background */
    .bg-grid {
        position: absolute;
        inset: 0;
        background-image: 
            linear-gradient(to right, rgba(0,0,0,0.035) 1px, transparent 1px),
            linear-gradient(to bottom, rgba(0,0,0,0.035) 1px, transparent 1px);
        background-size: 80px 80px;
        background-position: center center;
        z-index: 0;
        pointer-events: none;
    }
    
    /* Bottom Right Gradient Splash */
    .bottom-gradient-splash {
        position: absolute;
        bottom: -20vh;
        right: -10vw;
        width: 80vw;
        height: 40vh;
        background: linear-gradient(120deg, rgba(123, 226, 245, 0.4), rgba(228, 230, 155, 0.4), rgba(197, 164, 247, 0.4));
        filter: blur(80px);
        z-index: 0;
        pointer-events: none;
        transform: rotate(-15deg);
    }

    .card-section {
        position: relative;
        z-index: 10;
        width: 100%;
        max-width: 680px;
        padding: 0 20px;
    }

    .stepper-card {
        background: #ffffff;
        border-radius: 12px;
        box-shadow: 
            0 2px 5px rgba(0,0,0,0.02),
            0 15px 35px rgba(10, 25, 47, 0.05),
            0 40px 80px rgba(10, 25, 47, 0.04),
            0 0 0 1px rgba(10, 25, 47, 0.04);
        padding: 40px 48px;
    }

    /* Stepper Header */
    .stepper-header {
        display: flex;
        justify-content: space-between;
        margin-bottom: 16px;
    }

    .step {
        display: flex;
        align-items: center;
        gap: 8px;
        opacity: 0.4;
        transition: opacity 0.3s;
    }

    .step.active {
        opacity: 1;
    }

    .step-indicator {
        width: 14px;
        height: 14px;
        border-radius: 50%;
        border: 2px solid var(--color-blue-deep, #0a192f);
        background: transparent;
    }

    .step.active .step-indicator {
        border: 2px solid var(--color-blue-deep, #0a192f);
        box-shadow: inset 0 0 0 2px #fff, inset 0 0 0 4px var(--color-blue-deep, #0a192f);
        background: var(--color-blue-deep, #0a192f);
    }

    .step-label {
        font-size: 0.85rem;
        font-weight: 600;
        color: var(--color-blue-deep, #0a192f);
    }

    .stepper-progress {
        width: 100%;
        height: 3px;
        background: rgba(10, 25, 47, 0.05);
        border-radius: 2px;
        margin-bottom: 48px;
        overflow: hidden;
    }

    .progress-bar {
        height: 100%;
        background: var(--color-blue-deep, #0a192f); 
        border-radius: 2px;
        transition: width 0.3s ease;
    }

    /* Step Content */
    .step-content {
        padding: 0 4px;
    }

    .step-title {
        font-size: 1.5rem;
        font-weight: 700;
        color: var(--color-blue-deep, #0a192f);
        margin: 0 0 8px 0;
        letter-spacing: -0.01em;
    }

    .step-subtitle {
        font-size: 0.95rem;
        color: var(--color-text-muted, #555556);
        margin: 0 0 40px 0;
    }

    .minimal-form {
        display: flex;
        flex-direction: column;
        gap: 28px;
    }

    .form-row-horizontal {
        display: grid;
        grid-template-columns: 160px 1fr;
        align-items: center;
        gap: 24px;
    }

    .form-row-horizontal label {
        font-size: 0.9rem;
        font-weight: 600;
        color: var(--color-blue-deep, #0a192f);
        line-height: 1.3;
    }

    .minimal-form input,
    .minimal-form select {
        width: 100%;
        padding: 10px 14px;
        font-family: var(--font-sans);
        font-size: 0.95rem;
        color: var(--color-text, #050505);
        background-color: #f6f9fc;
        border: 1px solid transparent;
        border-radius: 6px;
        transition: all 0.2s ease;
        outline: none;
    }

    .minimal-form input::placeholder {
        color: #8792a2;
    }

    .minimal-form input:focus,
    .minimal-form select:focus {
        background-color: #ffffff;
        border-color: rgba(10, 25, 47, 0.3);
        box-shadow: 0 0 0 3px rgba(10, 25, 47, 0.08); 
    }

    .select-wrapper {
        position: relative;
    }

    .minimal-form select {
        appearance: none;
        cursor: pointer;
        padding-right: 36px;
    }

    .select-arrow {
        position: absolute;
        right: 12px;
        top: 50%;
        transform: translateY(-50%);
        width: 14px;
        height: 14px;
        color: var(--color-text-muted, #555556);
        pointer-events: none;
    }

    .form-actions {
        display: flex;
        justify-content: flex-end;
        margin-top: 16px;
    }

    .continue-btn {
        display: flex;
        align-items: center;
        gap: 6px;
        background-color: var(--color-blue-deep, #0a192f);
        color: #ffffff;
        font-family: var(--font-sans);
        font-size: 0.9rem;
        font-weight: 600;
        border: none;
        border-radius: 20px;
        padding: 10px 20px;
        cursor: pointer;
        transition: background-color 0.2s ease, transform 0.1s ease;
        box-shadow: 0 2px 6px rgba(10, 25, 47, 0.15);
    }

    .continue-btn:hover {
        background-color: #050d1a;
    }

    .continue-btn:active {
        transform: scale(0.98);
    }

    .continue-btn svg {
        width: 14px;
        height: 14px;
    }

    /* RESPONSIVE */
    @media (max-width: 650px) {
        .form-row-horizontal {
            grid-template-columns: 1fr;
            gap: 8px;
        }
        
        .stepper-header {
            flex-direction: column;
            gap: 12px;
            align-items: flex-start;
        }

        .stepper-card {
            padding: 30px 24px;
        }

        .minimal-header {
            padding: 20px;
        }
    }
</style>

<script>
    import { gsap } from 'gsap';
    
    document.addEventListener('DOMContentLoaded', () => {
        const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
        if (reduced) {
            gsap.set('.reveal-mockup', { opacity: 1, y: 0 });
            return;
        }

        // Animación de entrada suave del card central
        const card = document.querySelector<HTMLElement>('.reveal-mockup');
        if (card) {
            gsap.set(card, { y: 20, scale: 0.98, opacity: 0 });
            gsap.to(card, { 
                opacity: 1, y: 0, scale: 1, duration: 1.0, ease: 'power3.out', delay: 0.1 
            });
        }
    });
</script>
